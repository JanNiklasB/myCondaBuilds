name: binder_conda_package
on:
#   push:
#     branches: 
#       - master
#       - releases/**
#     paths:
#       - ".github/workflows/binder_conda_package.yml"
#       - "**.cpp"
#       - "**.h"
#       - "**.py"
#       - "**.i"
#       - "**/CMakeLists.txt"
#       - "conda-build/*"
#   release:
#     types: [published]
  workflow_dispatch:

jobs:
  arm:
    runs-on: ubuntu-arm
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Install Miniconda
        run: |
          set -ex
          ldd --version
          cd ~
          wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-aarch64.sh &> /dev/null
          sh Miniconda3-latest-Linux-aarch64.sh -b
      - name: Install needed global packages
        run: |
          sudo apt update
          sudo apt install -y clang-19 llvm-19 libclang-19-dev llvm-19-dev cmake ninja-build pybind11-dev gcc g++ git
      - name: Install Additional Conda Packages
        # the path is reseted in each new step:
        run: |
          source ~/miniconda3/bin/activate
          set -ex
          conda init &> /dev/null
          conda update -n base conda -y &> /dev/null
          conda install conda-build anaconda-client -y &> /dev/null
      - name: Build Conda Package
        env:
          accountName: ${{ secrets.ANACONDA_ACCOUNTNAME }}
          token: ${{ secrets.ANACONDA_TOKEN }}
        run: |
          source ~/miniconda3/bin/activate
          set -ex
          conda init &> /dev/null
          cd binder
          conda config --set anaconda_upload yes
          conda build -c conda-forge . -k \
            --user $accountName \
            --token $token \
            | tee build.log
          if [ $(find ~/miniconda3/conda-bld/linux-aarch64 -name "binder*" | wc -l) = 1 ]; then echo Success! ; else exit 1; fi
      - name: Archive Build Logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: "Anaconda_Build_ARM_Log"
          path: binder/build.log

  x64:
    runs-on: ubuntu
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Install needed global packages
        run: |
          sudo apt update
          sudo apt install -y clang-19 llvm-19 libclang-19-dev llvm-19-dev cmake ninja-build pybind11-dev gcc g++ git
      - name: Preinstall
        run: |
          set -ex
          conda init &> /dev/null
          conda update -n base conda -y &> /dev/null
          conda install conda-build anaconda-client -y &> /dev/null
      - name: Build Conda Package
        env:
          accountName: ${{ secrets.ANACONDA_ACCOUNTNAME }}
          token: ${{ secrets.ANACONDA_TOKEN }}
        run: |
          set -ex
          conda init &> /dev/null
          cd binder
          conda config --set anaconda_upload yes
          conda build -c conda-forge . -k \
            --user $accountName \
            --token $token \
            | tee build.log
          if [ $(find /usr/share/miniconda/conda-bld/linux-64 -name "binder*" | wc -l) = 1 ]; then echo Success! ; else exit 1; fi
      - name: Archive Build Logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: "Anaconda_Build_x64_Log"
          path: binder/build.log

  macos:
    runs-on: macos-14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Install Miniconda
        run: |
          set -ex
          cd ~
          curl -O https://repo.anaconda.com/miniconda/Miniconda3-latest-MacOSX-arm64.sh &> /dev/null
          sh Miniconda3-latest-MacOSX-arm64.sh -b
      - name: Install Additional Conda Packages
        run: |
          source ~/miniconda3/bin/activate
          set -ex
          conda init &> /dev/null
          conda update -n base conda -y &> /dev/null
          conda install -c conda-forge conda-build anaconda-client -y &> /dev/null
      - name: Build Conda Package
        env:
          accountName: ${{ secrets.ANACONDA_ACCOUNTNAME }}
          token: ${{ secrets.ANACONDA_TOKEN }}
          FAST_WAVES: "OFF"
          SIMD_EXTENSIONS: "none"
        run: |
          source ~/miniconda3/bin/activate
          set -ex
          cd binder
          conda config --set anaconda_upload yes
          conda build -c conda-forge -c janniklasb . -k \
            --user $accountName \
            --token $token \
            | tee build.log
          if [ $(find ~/miniconda3/conda-bld/osx-arm64 -name "binder*" | wc -l ) = 1 ]; then echo Success! ; else exit 1; fi
      - name: Archive Build Logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: "Anaconda_Build_MACOS_Log"
          path: binder/build.log